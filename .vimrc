" pretty colors
syntax on

" display relative line number
set relativenumber

" identing
set autoindent

" map gd for tag command (a bit tricky on a Norwegian keyboard)
nmap gd <C-]>

" searching
set ignorecase
set showmatch
" set incsearch

" paste toogle
" set pastetoggle=<F10>
